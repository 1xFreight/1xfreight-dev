export enum QuoteStatusEnum {
  REQUESTED = 'requested',
  BOOKED = 'booked',
  DISPATCHED = 'dispatched',
  AT_PICKUP = 'at pickup',
  IN_TRANSIT = 'in_transit',
  AT_DESTINATION = 'at_destination',
  DELIVERED = 'delivered',
  CANCELED = 'canceled',
}
